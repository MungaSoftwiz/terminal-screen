val draw_at_cursor : int array -> unit
(** [draw_at_cursor positions] draws at the cursor position specified by the [positions] array.
    Each element in the array represents a position where drawing should occur.

    @param positions An array of integers representing the positions to draw at.
    @return unit
*)
